export interface HealthDto {
    id: string;
    lifeMin: number;
    lifeMax: number;
    health: string;
    description: string;
    createdAt: string;
    updatedAt: string;
    publishedAt: string;
}
